<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>Login</title>
</head>
<body>

<h2>Login</h2>

<div class="login-container">
    <!-- Форма авторизації -->
    <form method="POST" th:action="@{/login}">
        
        <div class="form-group">
            <label for="username">Email</label>
            <input id="username" name="username" placeholder="Enter your email" required
                   type="email">
        </div>

        <div class="form-group">
            <label for="password">Password</label>
            <input id="password" name="password" placeholder="Enter your password" required
                   type="password">
        </div>

        <!-- Опція 'Запам'ятати мене' -->
        <div class="form-group">
            <input id="remember-me" name="remember-me" type="checkbox">
            <label for="remember-me">Remember me</label>
        </div>

        <!-- Виведення помилки, якщо авторизація невдала -->
        <div class="error-message" th:if="${param.error}">Invalid username or password</div>

        <div class="form-group">
            <button type="submit">Login</button>
        </div>
    </form>

    <!-- Посилання для реєстрації -->
    <p>Don't have an account? <a th:href="@{'/signup'}">Sign up</a></p>
</div>

</body>
</html>
