<!DOCTYPE html>
<html lang="uk" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>Редагувати користувача</title>
</head>
<body>
<h1>Редагувати користувача</h1>

<!-- Виводимо помилки, якщо є -->
<div th:if="${validationErrors != null}">
    <ul style="color: red;">
        <li th:each="error : ${validationErrors}" th:text="${error}"></li>
    </ul>
</div>

<form method="post" th:action="@{/dashboard/users/edit/{id}(id=${user.id})}" th:object="${user}">
    <div>
        <label for="email">Електронна пошта:</label>
        <input id="email" required th:field="*{email}" type="email">
    </div>
    <div>
        <label for="password">Пароль:</label>
        <input id="password" name="password" type="password">
        <small>Залиште пустим, якщо не хочете змінювати пароль</small>
    </div>
    <div>
        <label for="role">Роль:</label>
        <input id="role" required th:field="*{role}" type="text">
    </div>
    <button type="submit">Зберегти зміни</button>
    <a href="/dashboard/users">Назад до списку користувачів</a>
</form>
</body>
</html>
