<!DOCTYPE html>
<html lang="uk" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>Створити користувача</title>
</head>
<body>
<h1>Створити користувача</h1>

<!--/*@thymesVar id="createUserRequest" type="com.arakviel.eventlistenerdemo.dto.CreateUserRequest"*/-->
<form method="post" th:action="@{/dashboard/users/create}" th:object="${createUserRequest}">
    <div>
        <label for="email">Електронна пошта:</label>
        <input id="email" required th:field="*{email}" type="email">
    </div>
    <div>
        <label for="password">Пароль:</label>
        <input id="password" required th:field="*{password}" type="password">
    </div>
    <div>
        <label for="role">Роль:</label>
        <input id="role" required th:field="*{role}" type="text">
    </div>
    <button type="submit">Створити</button>
    <a href="/dashboard/users">Назад до списку користувачів</a>
</form>
</body>
</html>
